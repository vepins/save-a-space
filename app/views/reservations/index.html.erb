<p id="notice"><%= notice %></p>

<h1>Reservations</h1>

<table class="table">
  <thead>
    <tr>
      <th>User</th>
      <th>Room</th>
      <th>Start date hour</th>
      <th>End date hour</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reservations.each do |reservation| %>
      <tr>
        <td><%= reservation.user_id %></td>
        <td><%= reservation.room_id %></td>
        <td><%= reservation.start_date_hour %></td>
        <td><%= reservation.end_date_hour %></td>
        <td><%= link_to 'Show', class: "btn btn-info btn-sm", reservation %></td>
        <td><%= link_to 'Edit', class: "btn btn-warning btn-sm", edit_reservation_path(reservation) %></td>
        <td><%= link_to 'Destroy', reservation, class: "btn btn-danger btn-sm", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Reservation', new_reservation_path %>
